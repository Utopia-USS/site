import { appThemeInstance } from "../../../AppTheme";

const DotAnimeSettings = {
  radiuses: [10, 15],
  colors: [appThemeInstance.palette.primary.light, appThemeInstance.palette.secondary.light],
  coverage: 0.08,
}

export default DotAnimeSettings;